An anti-fraud image analyzer that:

- uses AI to pick images (disregarding cardholder information) from legal tests;
- or user input and;
- return the validity of the image (whether this is a card img or something else).


The app solution contains:

app.py
azure_blob.py
ocr_service.py
card_validator.py
