# creditcard_ai_validator
An anti-fraud image analyzer that uses AI to pick images (disregarding cardholder information) from legal tests or user input and return the validity of the image (whether this is a card img or something else)
